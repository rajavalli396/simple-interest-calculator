# Simple Interest Calculator

This project is a shell script that calculates simple interest based on user input. It prompts the user to enter the principal amount, rate of interest, and time period, and then computes the interest using the formula:

**Simple Interest = (Principal × Rate × Time) / 100**

## How to Run

To run the script, use the following command in your terminal:

```bash
bash simple-interest.sh
